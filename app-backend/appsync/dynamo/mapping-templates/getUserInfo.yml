request: |
  {
    "version" : "2017-02-28",
    "operation" : "Query",
    "query" : {
        "expression": "handle = :handle",
        "expressionValues" : {
            ":handle" : {
                "S" : "${context.arguments.handle}"
            }
        }
    }
  }

response: |
  $util.toJson($context.result.items)
